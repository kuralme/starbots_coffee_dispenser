version: '3'
services:
  starbots-manipulation:
    image: starbots-ros2-manipulation-real
    container_name: starbots-ros2-manipulation-real
    environment:
      - DISPLAY=${DISPLAY}
      - QT_X11_NO_MITSHM=1
      - ROS_DOMAIN_ID=0
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
    privileged: true
    networks:
      - starbots-network
    ports:
      - "9090:9090"
    restart: always
    entrypoint: /ros_entrypoint.sh
    command: ["ros2 launch starbots_bringup starbots_coffee_delivery.launch.py & \
      sleep 5 && ros2 launch starbots_webapp start_foxglove.launch.xml > /dev/null 2>&1"]

volumes:
  logs: {}
networks:
  starbots-network:
    driver: bridge